#include "LTexture.h"
#include <iostream>
#include <list>

using namespace std;

class BackgroundTemplate
{
    public:
		int x;
		int y;
		LTexture textura;

    private:
		BackgroundTemplate(SDL_Texture* gRenderer,x,y,path);
		void render();	
};
